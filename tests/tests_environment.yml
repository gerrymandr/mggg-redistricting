name: adj
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - matplotlib
  - pandas
  - pysal
  - palettable
  - fiona
  - notebook
  # this is just to reduce download size.  It can be safely removed, but will slow down CI.
  - nomkl
  - pytest
